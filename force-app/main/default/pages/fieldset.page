<apex:page standardController="Account" >
    <apex:form >
        <apex:pageBlock >
            <apex:pageBlockSection >
                <apex:repeat value="{!$ObjectType.Account.fieldsets.New_One}" var="fieldValue">
                    <apex:inputField value="{!Account[fieldValue]}" />
                </apex:repeat>
            </apex:pageBlockSection>
        </apex:pageBlock>
    </apex:form>
</apex:page>